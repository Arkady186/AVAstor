# Инструкция по деплою на Render

## Подготовка

1. Создайте аккаунт на [Render.com](https://render.com)
2. Подключите ваш GitHub репозиторий к Render

## Настройка базы данных

1. В Render Dashboard создайте новую PostgreSQL базу данных:
   - Name: `avastore-db`
   - Database: `avastore`
   - User: `avastore_user`
   - Plan: Starter (или выше)

2. Скопируйте Internal Database URL (он будет использован автоматически)

## Настройка Backend

1. Создайте новый Web Service:
   - Name: `avastore-backend`
   - Environment: `Node`
   - Build Command: `cd backend && npm install && npm run build`
   - Start Command: `cd backend && npm start`
   - Plan: Starter (или выше)

2. Добавьте Environment Variables:
   - `NODE_ENV` = `production`
   - `PORT` = `3000`
   - `DATABASE_URL` = (Internal Database URL из базы данных)
   - `TELEGRAM_BOT_TOKEN` = (токен вашего Telegram бота от @BotFather)
   - `JWT_SECRET` = (случайная строка для JWT токенов)
   - `FRONTEND_URL` = (URL вашего фронтенда после деплоя, например: `https://avastore-frontend.onrender.com`)

3. Health Check Path: `/health`

## Настройка Frontend

1. Создайте новый Static Site:
   - Name: `avastore-frontend`
   - Build Command: `cd frontend && npm install && npm run build`
   - Publish Directory: `frontend/dist`

2. Добавьте Environment Variables:
   - `VITE_API_URL` = (URL вашего бэкенда, например: `https://avastore-backend.onrender.com`)

## Настройка Telegram бота

1. Создайте бота через [@BotFather](https://t.me/BotFather) в Telegram
2. Получите токен бота
3. Настройте Web App:
   ```
   /newapp
   ```
   - Выберите вашего бота
   - Название: `AvaStore`
   - Описание: `Маркетплейс AvaStore`
   - Фото: (опционально)
   - Web App URL: `https://avastore-frontend.onrender.com`
   - Short name: `avastore`

4. Добавьте кнопку в меню бота:
   ```
   /setmenubutton
   ```
   - Выберите вашего бота
   - Text: `🛍️ Открыть магазин`
   - Web App URL: `https://avastore-frontend.onrender.com`

## Альтернативный способ (через render.yaml)

Если вы используете `render.yaml`, Render автоматически создаст все сервисы:

1. Загрузите `render.yaml` в корень репозитория
2. В Render Dashboard выберите "New" → "Blueprint"
3. Подключите репозиторий
4. Render автоматически создаст все сервисы согласно конфигурации

## Проверка работы

1. Откройте вашего бота в Telegram
2. Нажмите кнопку "🛍️ Открыть магазин"
3. Мини-приложение должно открыться и работать

## Важные замечания

- Убедитесь, что все Environment Variables установлены правильно
- Frontend URL должен быть доступен по HTTPS
- Backend должен быть доступен по HTTPS для работы с Telegram Web App
- Проверьте, что база данных создана и доступна
- При первом запуске таблицы создадутся автоматически

