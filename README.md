## Telegram Mini App: INK&ARTstudio

Мини-приложение Telegram для студии татуировки INK&ARTstudio. Позволяет клиентам просматривать портфолио мастеров, выбирать услуги и записываться на сеансы.

### Основные возможности

- 🎨 **Портфолио работ** — галерея выполненных татуировок с фильтрацией по стилям
- 👨‍🎨 **Каталог мастеров** — информация о мастерах, их специализации и рейтингах
- 📅 **Запись на сеанс** — удобная система бронирования с выбором мастера, услуги, даты и времени
- 📋 **Мои записи** — управление записями, просмотр истории сеансов
- 👤 **Профиль** — информация о клиенте, статистика записей

### Локальный запуск

1. Установите Node.js 18+
2. Установите зависимости и соберите клиент:
   - В корне `miniapp`: `npm install`
3. Запуск сервера: `npm start`
4. Откройте `http://localhost:10000` (из браузера увидите блок-экран — это ожидаемо).

### Развёртывание через GitHub + Render

1. Создайте приватный репозиторий на GitHub и запушьте папку `miniapp` в корень репо.
2. На Render создайте Web Service:
   - Connect → выберите ваш репозиторий
   - Root Directory: `.`
   - Build Command: `npm install`
   - Start Command: `node server/index.js`
   - Environment: Node, `NODE_VERSION=18`
3. После билда получите домен вида `https://<service>.onrender.com` и добавьте его в BotFather в полях `Inline mode > Web App` или в кнопке.

### Проверка, что открыто в Telegram

- На стороне клиента: проверка наличия `window.Telegram.WebApp`, иначе — блок-экран.
- На стороне сервера: проверка User-Agent на `Telegram` и отдача блок-экранов для обычных браузеров.

### Переменные окружения

Скопируйте `.env.example` в `.env` и заполните при необходимости:

- `BOT_TOKEN` — токен бота (для проверки подписи initData)
- `APP_BASE_URL` — базовый URL приложения на Render

### Включение проверки initData на Render

1. Зайдите в Render → ваш Web Service → Environment → Add Environment Variable
2. Добавьте `BOT_TOKEN` со значением токена вашего бота
3. Redeploy (или Restart) сервиса
4. Клиент отправляет `initData` на `POST /api/verify`; сервер проверяет подпись и возвращает `user`

### Режим ограничения только для Telegram (опционально)

По умолчанию приложение доступно и в браузере на ПК. Чтобы включить строгую блокировку и разрешить только внутри Telegram:

- Установите переменную окружения `TELEGRAM_ONLY=1` на Render
- (Опционально для локальной отладки) `ALLOW_BROWSER=1` будет пропускать и обычный браузер

### Структура данных

Приложение использует следующие типы данных:
- **Master** — информация о мастерах (имя, специализация, рейтинг, портфолио)
- **Service** — услуги студии (татуировка, коррекция, консультация и т.д.)
- **PortfolioItem** — работы из портфолио
- **Appointment** — записи клиентов на сеансы

Данные сохраняются в localStorage для демонстрации. В продакшене рекомендуется использовать базу данных.
