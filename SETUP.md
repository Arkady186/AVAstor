# Инструкция по настройке проекта

## Предварительные требования

- Node.js 18+ и npm
- PostgreSQL (для локальной разработки)
- Telegram бот (созданный через @BotFather)

## Локальная разработка

### 1. Клонирование и установка

```bash
# Клонируйте репозиторий
git clone <your-repo-url>
cd avastore

# Установите зависимости для всех частей проекта
npm run install:all
```

### 2. Настройка базы данных

1. Создайте базу данных PostgreSQL:
```sql
CREATE DATABASE avastore;
```

2. Создайте файл `backend/.env` на основе `backend/.env.example`:
```bash
cp backend/.env.example backend/.env
```

3. Заполните переменные окружения в `backend/.env`:
```env
PORT=3000
NODE_ENV=development
DATABASE_URL=postgresql://user:password@localhost:5432/avastore
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
JWT_SECRET=your_random_secret_key_here
FRONTEND_URL=http://localhost:5173
```

### 3. Настройка фронтенда

1. Создайте файл `frontend/.env`:
```env
VITE_API_URL=http://localhost:3000
```

### 4. Запуск проекта

В корне проекта:
```bash
npm run dev
```

Или отдельно:
```bash
# Backend
npm run dev:backend

# Frontend (в другом терминале)
npm run dev:frontend
```

### 5. Настройка Telegram бота

1. Создайте бота через [@BotFather](https://t.me/BotFather):
   - Отправьте `/newbot`
   - Следуйте инструкциям
   - Сохраните токен бота

2. Настройте Web App для бота:
   ```
   /newapp
   ```
   - Выберите вашего бота
   - Название: `AvaStore`
   - Описание: `Маркетплейс AvaStore`
   - Web App URL: `https://your-domain.com` (для продакшена) или используйте ngrok для локальной разработки
   - Short name: `avastore`

3. Для локальной разработки используйте ngrok:
   ```bash
   # Установите ngrok
   # Запустите ngrok для фронтенда
   ngrok http 5173
   
   # Используйте полученный URL в настройках Web App
   ```

### 6. Проверка работы

1. Откройте вашего бота в Telegram
2. Нажмите кнопку "🛍️ Открыть магазин" или отправьте `/start`
3. Мини-приложение должно открыться

## Структура проекта

```
avastore/
├── backend/              # Backend API (Node.js + Express + TypeScript)
│   ├── src/
│   │   ├── database/     # База данных и миграции
│   │   ├── middleware/    # Middleware (auth, error handling)
│   │   ├── routes/        # API routes
│   │   ├── telegram/      # Telegram bot
│   │   └── index.ts       # Точка входа
│   ├── package.json
│   └── tsconfig.json
├── frontend/             # Frontend (React + TypeScript + Vite)
│   ├── src/
│   │   ├── components/   # React компоненты
│   │   ├── pages/         # Страницы
│   │   ├── services/      # API сервисы
│   │   ├── utils/         # Утилиты
│   │   └── App.tsx        # Главный компонент
│   ├── package.json
│   └── vite.config.ts
├── package.json          # Root package.json
├── render.yaml           # Конфигурация для Render
└── README.md
```

## Полезные команды

```bash
# Установка всех зависимостей
npm run install:all

# Запуск в режиме разработки
npm run dev

# Сборка проекта
npm run build

# Запуск только backend
npm run dev:backend

# Запуск только frontend
npm run dev:frontend
```

## Решение проблем

### База данных не подключается
- Проверьте, что PostgreSQL запущен
- Проверьте правильность DATABASE_URL в `.env`
- Убедитесь, что база данных создана

### Telegram бот не работает
- Проверьте правильность TELEGRAM_BOT_TOKEN
- Убедитесь, что бот запущен (должен быть виден в логах backend)

### Frontend не подключается к backend
- Проверьте, что backend запущен на порту 3000
- Проверьте VITE_API_URL в `frontend/.env`
- Проверьте CORS настройки в backend

## Следующие шаги

1. Добавьте тестовые данные в базу данных
2. Настройте загрузку изображений
3. Добавьте систему отзывов
4. Настройте платежную систему
5. Добавьте админ-панель

