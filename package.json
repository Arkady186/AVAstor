{
  "name": "tg-miniapp-shop",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "postinstall": "npm --prefix client ci || npm --prefix client install && npm --prefix client run build",
    "build": "npm --prefix client run build",
    "dev": "node server/index.js",
    "start": "node server/index.js"
  },
  "dependencies": {
    "compression": "^1.7.4",
    "express": "^4.19.2",
    "helmet": "^7.1.0"
  }
}




